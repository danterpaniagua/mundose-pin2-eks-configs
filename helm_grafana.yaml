datasources:
  datasources.yaml:
    apiVersion: v1
    datasources:
    - access: proxy
      name: Prometheus
      type: prometheus
      url: prometheus-server.prometheus.svc.cluster.local
      isDefault: true